package com.diegodelacruz.codesignal.stringmanipulation;

/**
 * Given two strings, source and target, of the same length n (1≤n≤500)
 * inclusive, both comprise only lowercase English letters.
 * The function should return the zero-based index of the swap in the source string if
 * exactly one swap of consecutive elements can fix the target string to match the source string;
 * otherwise, the function should return -1. The index of the swap is the index of the left element
 * in the swapped pair.
 */
public class SubstringSwap {

    /**
     * Note:
     * Characters can be swapped at most once.
     * Don't check for swaps at the last position of the string, since there is no character with which to swap.
     * If multiple swaps are needed, or if no swaps are needed, or it's impossible to fix via swaps, return -1.
     * @param source
     * @param target
     * @return
     */
    public int spotSwaps(String source, String target) {
        // TODO: implement
        return -1;
    }

    /**
     * Example
     * For source = "hello" and target = "hlelo", the output should be 1.
     * @param args
     */
    public static void main(String[] args) {

    }


}
